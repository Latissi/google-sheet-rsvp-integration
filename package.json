{
  "name": "google-sheet-rsvp-integration",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --runInBand",
    "build": "rm -rf dist && node build.js && cp appsscript.json dist/",
    "push:dev": "npm run build && cp .clasp-dev.json .clasp.json && clasp push",
    "push:prod": "npm run build && cp .clasp-prod.json .clasp.json && clasp push"
  },
  "devDependencies": {
    "@types/google-apps-script": "^2.0.8",
    "@types/jest": "^30.0.0",
    "esbuild": "^0.27.4",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}
